name: PR Test

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  approval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
      - run: node --version
      - run: pnpm test
